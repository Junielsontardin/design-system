import { Box, BoxProps } from '@junielson-ui/react'
import { StoryObj, Meta } from '@storybook/react'

export default {
  title: 'Box',
  component: Box,
} as Meta<BoxProps>

export const Primary: StoryObj<BoxProps> = {}
